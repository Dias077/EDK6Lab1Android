# EDK
Простейшая программа суммы двух цифр на Android

![Screenshot](edk.png)

```
if (x >= 4)
            c = (x + (4 * a)) / (a * a) * (b * b);
            else
            c = (x * x * x) - (a*b);
```
